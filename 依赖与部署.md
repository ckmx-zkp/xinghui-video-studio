# 星绘视频工坊依赖与部署

## 1. 部署结构

项目包含两个可以分开验证的部分：

- Web/Agent：React、Node.js、MiniMax M3、项目与素材管理、创作流程。
- GPU/H3：ComfyUI、MiniMax H3 模型、本地视频生成。

没有 GPU/H3 时，Web/Agent 仍应正常运行；只有本地开拍不可用。

## 2. 软件依赖

目标电脑需要：

- Windows 10/11 x64。
- Git。
- Node.js `^20.19.0` 或 `>=22.12.0`，推荐 Node.js 22 LTS 或当前已验证的 Node.js 24。
- npm，随 Node.js 安装。
- OpenSSH Client，仅在使用阿里云反向 SSH 时需要。
- 支持 WebP/MP4 的现代浏览器。

`ffmpeg-static` 随 npm 依赖安装，不要求系统单独安装 FFmpeg。如果目标电脑无法运行该二进制，可安装可用的 Windows x64 FFmpeg，并在 `.env` 的 `FFMPEG_PATH` 中填写 `ffmpeg.exe` 绝对路径。

## 3. 获取 company 分支

首次部署：

```powershell
git clone -b company https://github.com/ckmx-zkp/xinghui-video-studio.git xinghui-video-studio
cd xinghui-video-studio
```

如果目标电脑已经克隆过仓库：

```powershell
git fetch origin
git switch company
git pull --ff-only origin company
```

注意：只有把本机 `company` 分支推送到 GitHub 后，另一台电脑才能按上述命令拉取。

## 4. 安装与配置 Web/Agent

安装锁定依赖并构建：

```powershell
cd video-studio
npm.cmd ci
npm.cmd run build
cd ..
Copy-Item .env.example .env
```

编辑根目录 `.env`：

```dotenv
MINIMAX_API_KEY=你的密钥
MINIMAX_API_HOST=https://api.minimaxi.com
MINIMAX_TEXT_MODEL=MiniMax-M3
STUDIO_PORT=4175
COMFY_URL=http://127.0.0.1:8188
FFMPEG_PATH=
STUDIO_VIDEO_DEMO_MODE=0
STUDIO_DIRECTOR_DEMO_MODE=0
```

不要提交 `.env`。启动 Web 工坊：

```powershell
.\启动_星绘视频工坊.bat
```

浏览器访问 `http://127.0.0.1:4175`。

## 5. GPU/H3 依赖

本地生成要求根目录存在：

```text
ComfyUI_windows_portable/
  ComfyUI/
    main.py
    models/
    custom_nodes/
```

模型必须放在以下位置：

| 模型 | 相对目录 |
|---|---|
| `minimax_h3_fl2va_pruned-Q4_K.gguf` | `ComfyUI/models/diffusion_models/` |
| `qwen3vl-32B-MiniMax-H3-Q2_K.gguf` | `ComfyUI/models/text_encoders/` |
| `minimax_h3_video_vae_int8_convrot.safetensors` | `ComfyUI/models/vae/` |
| `minimax_h3_audio_vae_bf16.safetensors` | `ComfyUI/models/vae/` |
| `minimax_h3_turbo_v4_step600_ema.safetensors` | `ComfyUI/models/loras/` |

ComfyUI 还必须提供这些工作流节点：

- `UnetLoaderGGUF`
- `CLIPLoaderGGUF`
- `MiniMaxH3ImageToVideo`
- `MiniMaxH3TurboLoRA`
- `MiniMaxH3TurboSampler`
- `VAEDecodeAudio`
- `CreateVideo`
- `SaveVideo`

自定义节点和模型体积很大，不通过 Git 仓库分发。优先从已经跑通的 GPU 电脑复制完整 `ComfyUI_windows_portable`，或者在目标 GPU 电脑按 H3 官方流程重新部署。

## 6. 分层验收

无 GPU 电脑可以验收：

- M3 多轮需求访谈。
- 每轮导演见解、多个选项和自由回答。
- 创作简报、制作计划、三套创意方向。
- 分镜生成、质量审核、阶段门禁。
- 项目历史、参考素材保存与跨项目复用。
- TypeScript 构建、lint、单元与 mock 集成测试。

GPU 电脑还需验收：

- `/api/status` 返回 `comfy: true` 和 `models.ready: true`。
- 参考图上传后没有跨镜头串图。
- H3 排队、轮询、失败和完成状态。
- 单镜实际画质、显存峰值和耗时。
- 多镜头 FFmpeg 合片、音轨和最终交付。

云端 Hailuo 与本地 GPU 无关，但每个镜头会消耗额度。常规部署检查不要提交真实云端任务。

## 7. 常用检查

```powershell
cd video-studio
npm.cmd run build
npm.cmd run lint
npm.cmd test
Invoke-RestMethod http://127.0.0.1:4175/api/status
```

常见状态：

- `m3: false`：根目录 `.env` 未配置有效 `MINIMAX_API_KEY`。
- `comfy: false`：8188 未监听，或 `COMFY_URL` 不正确。
- `models.ready: false`：至少一个 H3 模型不在约定目录。
- 页面可打开但本地不能开拍：属于 GPU/H3 层未就绪，不影响前置创作流程测试。

## 8. 无 GPU 测试模式

无 GPU 但有 MiniMax API Key 时，在测试电脑的 `.env` 中设置：

```dotenv
MINIMAX_API_KEY=你的真实密钥
STUDIO_VIDEO_DEMO_MODE=1
STUDIO_DIRECTOR_DEMO_MODE=0
```

重启 Node 服务后，导演访谈、见解、选项、创意方向和分镜会调用真实 MiniMax M3；只有本地视频开拍使用模拟结果。页面会分别显示“MiniMax-M3 已连接”和“本机生成 模拟模式”。

完全离线、只验证 UI 和阶段流转时才设置：

```dotenv
STUDIO_VIDEO_DEMO_MODE=1
STUDIO_DIRECTOR_DEMO_MODE=1
```

此时导演回复是固定测试数据，不是 LLM 推理，页面会明确显示“导演 固定演示”。正式部署必须把两个开关都改回 `0`。旧的 `STUDIO_DEMO_MODE=1` 仅作为 `STUDIO_VIDEO_DEMO_MODE=1` 的兼容写法，不再伪造导演连接状态。
